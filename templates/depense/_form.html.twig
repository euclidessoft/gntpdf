{{ form_start(form, {'attr': {'id': 'form'}}) }}

<div class="col-lg-5 offset-lg-2 mt-4 fond-7 pb-5 pt-3 px-5">
    <h4 class="mb-3">
        <i class="fa-brands fa-product-hunt me-2"></i><i class="fa fa-plus me-2"></i>
        {{ 'Nouvelle dépense'|trans }}
    </h4>

    {% for message in app.flashes('notice') %}
    <div role="alert" class="alert alert-success alert-dismissible fade show radius-10 shadow-black">
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        {{ message|trans }}
    </div>
    {% endfor %}

    <div class="row">

        <div class="col-12 mb-4">
            {{ form_row(form.libelle, {'attr': {'placeholder': 'Libellé *'}, 'label': 'Libellé dépense *'}) }}
        </div>

        <div class="col-12 mb-4">
            {{ form_row(form.categorie, {'attr': {'placeholder': 'Ligne de dépense *'}, 'label': 'Ligne de dépense *'}) }}
        </div>

        <div class="col-12 mb-4">
            {{ form_row(form.montant, {'attr': {'placeholder': 'Montant *'}, 'label': 'Montant *'}) }}
        </div>

        <div class="col-12 mb-4">
          {{ form_row(form.type, {'label':'Type de dépense', 'attr' : {'onchange' : 'typeversement()'}}) }}
        </div>

    </div>
    <div id="versement">
        <div class="form-group">
            {{ form_row(form.banque, {'label':'Banque', 'attr' : {'placeholder':'Banque'}}) }}
        </div>
        <div class="form-group mt-4">
            {{ form_row(form.numero, {'label':'Numéro transaction', 'attr' : {'placeholder':'Numéro'}}) }}
        </div>
    </div>
    {{ form_rest(form) }}
</div>


{% block scripts %}
<script>

    function typeversement() {
        if ($('#depense_type option:selected').val() == 'Cheque' || $('#depense_type option:selected').val() == 'Virement') {
            $('#versement').show();
            $('#depense_numero').val();
        } else {
            $('#versement').hide();
            $('#depense_numero').val();
        }
    }
</script>

{% endblock %}