<!DOCTYPE html>
<html lang="{{ app.request.locale }}">

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta name="description" content="GNT PHARNA est en ligne pour la vente, la distribution et la promotion de produits pharmaceutiques">
    <meta name="title" content="GNT PHARNA">
    <meta name="author" content="EUCLIDES">
    <title> {% block title %}  {% endblock %} </title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.5/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>

        .titre {
            font-family: Lato, Verdana, Geneva, Tahoma, sans-serif;
            font-size: 2rem;
            font-weight: bolder;
            color: #039c4f;
        }

        .text {
            font-size: 0.75rem;
        }

        .badge {
            background-color: red !important;
            display: inline-block;
            padding: 0.35em 0.65em;
            font-size: 0.75em;
            font-weight: 700;
            line-height: 1;
            color: #fff;
            text-align: center;
            white-space: nowrap;
            vertical-align: baseline;
            border-radius: 0.25rem;
        }

        table {
            border: 1.5px solid #000 !important;
            border-collapse: collapse;
        }

        table > :not(caption) > * > * {
            padding: 5px !important;
        }

        thead, th {
            background-color: #c5fbae !important;
            color: #000;
            vertical-align: middle;
        }

        th, td {
            border: 1.5px solid #000 !important;
        }

        .montant {
            text-align: right;
            white-space: nowrap !important;
        }

        @media print {
            @page {
                size: A4 portrait;
                margin: 0;
            }
            html {
                margin: 10mm;
            }

            .contenu {
                zoom: 80%;
            }

            *,
            *::before,
            *::after {
                -webkit-print-color-adjust: exact !important;
                print-color-adjust: exact !important;
                color-adjust: exact !important;   /* ancien nom (Firefox) */
            }
        }

    </style>

</head>
<body>
<div class="row" style="border-bottom: 1px solid #039c4f;padding-bottom: 20px;">
    <div class="col-2">
        <img src="https://gnt-pharma.com/images/gntpharma.jpeg" alt="Logo" class="w-100">
    </div>
    <div class="col-10 text-center">
        <div class="row">
            <div class="col-12 titre fw-bolder">
                GNT PHARMA
            </div>
            <div class="col-12 text">
                BP : 3582 Yaoundé – Cameroun, NGOUSSO
                <br>
                Montée Éleveur Immeuble Sainte Marie
            </div>
            <div class="col-12 text">
                +237 69 71 49 4 12&nbsp;&nbsp;&nbsp;-&nbsp;&nbsp;&nbsp;+237 67 52 47 865
            </div>
            <div class="col-12 text">
                info@gnt-pharma.com&nbsp;&nbsp;&nbsp;-&nbsp;&nbsp;&nbsp;www.gnt-pharma.com
            </div>
            <div class="col-12 text">
                RCCM N° : RC/YAE/2023/M/105&nbsp;&nbsp;&nbsp;-&nbsp;&nbsp;&nbsp;NIU : M122217881373Q
            </div>
        </div>
    </div>
</div>
{% block contenu %}

{% endblock %}

<script>

    document.addEventListener('DOMContentLoaded', function() {
        window.print();
        window.addEventListener('afterprint', function() {
            history.back();
        });
    });

    document.addEventListener('DOMContentLoaded', function () {

        var nombre = document.getElementById("total").innerText;
        var total = Math.floor(document.getElementById("total").innerText);
        var decimal = Math.round((nombre - total) * 100);
        var totalEnLettres = NumberToLetter(total);
        var virgule = NumberToLetter(decimal);
        document.getElementById("conversion").innerHTML = totalEnLettres;
        document.getElementById("virgule").innerHTML = " " + "{{ 'virgule'|trans }}" + " " + virgule;

    });

    /*****Start Script de conversion le prix en lettre*****/

    // Utilisation de la fonction NumberToLetter

    function Unite(nombre) {
        var unite;
        switch (nombre) {
            case 0:
                unite = "{{ 'zéro'|trans }}";
                break;
            case 1:
                unite = "{{ 'un'|trans }}";
                break;
            case 2:
                unite = "{{ 'deux'|trans }}";
                break;
            case 3:
                unite = "{{ 'trois'|trans }}";
                break;
            case 4:
                unite = "{{ 'quatre'|trans }}";
                break;
            case 5:
                unite = "{{ 'cinq'|trans }}";
                break;
            case 6:
                unite = "{{ 'six'|trans }}";
                break;
            case 7:
                unite = "{{ 'sept'|trans }}";
                break;
            case 8:
                unite = "{{ 'huit'|trans }}";
                break;
            case 9:
                unite = "{{ 'neuf'|trans }}";
                break;
        }//fin switch
        return unite;
    }//-----------------------------------------------------------------------

    function Dizaine(nombre) {
        switch (nombre) {
            case 10:
                dizaine = "{{ 'dix'|trans }}";
                break;
            case 11:
                dizaine = "{{ 'onze'|trans }}";
                break;
            case 12:
                dizaine = "{{ 'douze'|trans }}";
                break;
            case 13:
                dizaine = "{{ 'treize'|trans }}";
                break;
            case 14:
                dizaine = "{{ 'quatorze'|trans }}";
                break;
            case 15:
                dizaine = "{{ 'quinze'|trans }}";
                break;
            case 16:
                dizaine = "{{ 'seize'|trans }}";
                break;
            case 17:
                dizaine = "{{ 'dix-sept'|trans }}";
                break;
            case 18:
                dizaine = "{{ 'dix-huit'|trans }}";
                break;
            case 19:
                dizaine = "{{ 'dix-neuf'|trans }}";
                break;
            case 20:
                dizaine = "{{ 'vingt'|trans }}";
                break;
            case 30:
                dizaine = "{{ 'trente'|trans }}";
                break;
            case 40:
                dizaine = "{{ 'quarante'|trans }}";
                break;
            case 50:
                dizaine = "{{ 'cinquante'|trans }}";
                break;
            case 60:
                dizaine = "{{ 'soixante'|trans }}";
                break;
            case 70:
                dizaine = "{{ 'soixante-dix'|trans }}";
                break;
            case 71:
                dizaine = "{{ 'soixante-onze'|trans }}";
                break;
            case 72:
                dizaine = "{{ 'soixante-douze'|trans }}";
                break;
            case 73:
                dizaine = "{{ 'soixante-treize'|trans }}";
                break;
            case 74:
                dizaine = "{{ 'soixante-quatorze'|trans }}";
                break;
            case 75:
                dizaine = "{{ 'soixante-quinze'|trans }}";
                break;
            case 76:
                dizaine = "{{ 'soixante-seize'|trans }}";
                break;
            case 77:
                dizaine = "{{ 'soixante-dix-sept'|trans }}";
                break;
            case 78:
                dizaine = "{{ 'soixante-dix-huit'|trans }}";
                break;
            case 79:
                dizaine = "{{ 'soixante-dix-neuf'|trans }}";
                break;
            case 80:
                dizaine = "{{ 'quatre-vingt'|trans }}";
                break;
            case 90:
                dizaine = "{{ 'quatre-vingt-dix'|trans }}";
                break;
            case 91:
                dizaine = "{{ 'quatre-vingt-onze'|trans }}";
                break;
            case 92:
                dizaine = "{{ 'quatre-vingt-douze'|trans }}";
                break;
            case 93:
                dizaine = "{{ 'quatre-vingt-treise'|trans }}";
                break;
            case 94:
                dizaine = "{{ 'quatre-vingt-quatorze'|trans }}";
                break;
            case 95:
                dizaine = "{{ 'quatre-vingt-quinze'|trans }}";
                break;
            case 96:
                dizaine = "{{ 'quatre-vingt-seize'|trans }}";
                break;
            case 97:
                dizaine = "{{ 'quatre-vingt-dix-sept'|trans }}";
                break;
            case 98:
                dizaine = "{{ 'quatre-vingt-dix-huit'|trans }}";
                break;
            case 99:
                dizaine = "{{ 'quatre-vingt-dix-neuf'|trans }}";
                break;
        }//fin switch
        return dizaine;
    }//-----------------------------------------------------------------------

    function NumberToLetter(nombre) {
        var i, j, n, quotient, reste, nb;
        var ch
        var numberToLetter = '';
        //__________________________________

        if (nombre.toString().replace(/ /gi, "").length > 15) return "dépassement de capacité";
        if (isNaN(nombre.toString().replace(/ /gi, ""))) return "Nombre non valide";

        nb = parseFloat(nombre.toString().replace(/ /gi, ""));
        if (Math.ceil(nb) != nb) return "Nombre avec virgule non géré.";

        n = nb.toString().length;
        switch (n) {
            case 1:
                numberToLetter = Unite(nb);
                break;
            case 2:
                if (nb > 19) {
                    quotient = Math.floor(nb / 10);
                    reste = nb % 10;
                    if (nb < 71 || (nb > 79 && nb < 91)) {
                        if (reste == 0) numberToLetter = Dizaine(quotient * 10);
                        if (reste == 1) numberToLetter = Dizaine(quotient * 10) + "{{ '-et-'|trans }}" + Unite(reste);
                        if (reste > 1) numberToLetter = Dizaine(quotient * 10) + "-" + Unite(reste);
                    } else numberToLetter = Dizaine(nb);
                    // }else numberToLetter = Dizaine((quotient - 1) * 10) + "-" + Dizaine(10 + reste);
                } else numberToLetter = Dizaine(nb);
                break;
            case 3:
                quotient = Math.floor(nb / 100);
                reste = nb % 100;
                if (quotient == 1 && reste == 0) numberToLetter = "{{ 'cent'|trans }}";
                if (quotient == 1 && reste != 0) numberToLetter = "{{ 'cent'|trans }}" + " " + NumberToLetter(reste);
                if (quotient > 1 && reste == 0) numberToLetter = Unite(quotient) + " " + "{{ 'cents'|trans }}";
                if (quotient > 1 && reste != 0) numberToLetter = Unite(quotient) + " " + "{{ 'cent'|trans }}" + " " + NumberToLetter(reste);
                break;
            case 4 :
                quotient = Math.floor(nb / 1000);
                reste = nb - quotient * 1000;
                if (quotient == 1 && reste == 0) numberToLetter = "{{ 'mille'|trans }}";
                if (quotient == 1 && reste != 0) numberToLetter = "{{ 'mille'|trans }}" + " " + NumberToLetter(reste);
                if (quotient > 1 && reste == 0) numberToLetter = NumberToLetter(quotient) + " " + "{{ 'mille'|trans }}";
                if (quotient > 1 && reste != 0) numberToLetter = NumberToLetter(quotient) + " " + "{{ 'mille'|trans }}" + " " + NumberToLetter(reste);
                break;
            case 5 :
                quotient = Math.floor(nb / 1000);
                reste = nb - quotient * 1000;
                if (quotient == 1 && reste == 0) numberToLetter = "{{ 'mille'|trans }}";
                if (quotient == 1 && reste != 0) numberToLetter = "{{ 'mille'|trans }}" + " " + NumberToLetter(reste);
                if (quotient > 1 && reste == 0) numberToLetter = NumberToLetter(quotient) + " " + "{{ 'mille'|trans }}";
                if (quotient > 1 && reste != 0) numberToLetter = NumberToLetter(quotient) + " " + "{{ 'mille'|trans }}" + " " + NumberToLetter(reste);
                break;
            case 6 :
                quotient = Math.floor(nb / 1000);
                reste = nb - quotient * 1000;
                if (quotient == 1 && reste == 0) numberToLetter = "{{ 'mille'|trans }}";
                if (quotient == 1 && reste != 0) numberToLetter = "{{ 'mille'|trans }}" + " " + NumberToLetter(reste);
                if (quotient > 1 && reste == 0) numberToLetter = NumberToLetter(quotient) + " " + "{{ 'mille'|trans }}";
                if (quotient > 1 && reste != 0) numberToLetter = NumberToLetter(quotient) + " " + "{{ 'mille'|trans }}" + " " + NumberToLetter(reste);
                break;
            case 7:
                quotient = Math.floor(nb / 1000000);
                reste = nb % 1000000;
                if (quotient == 1 && reste == 0) numberToLetter = "{{ 'un million'|trans }}";
                if (quotient == 1 && reste != 0) numberToLetter = "{{ 'un million'|trans }}" + " " + NumberToLetter(reste);
                if (quotient > 1 && reste == 0) numberToLetter = NumberToLetter(quotient) + " " + "{{ 'millions'|trans }}";
                if (quotient > 1 && reste != 0) numberToLetter = NumberToLetter(quotient) + " " + "{{ 'millions'|trans }}" + " " + NumberToLetter(reste);
                break;
            case 8:
                quotient = Math.floor(nb / 1000000);
                reste = nb % 1000000;
                if (quotient == 1 && reste == 0) numberToLetter = "{{ 'un million'|trans }}";
                if (quotient == 1 && reste != 0) numberToLetter = "{{ 'un million'|trans }}" + " " + NumberToLetter(reste);
                if (quotient > 1 && reste == 0) numberToLetter = NumberToLetter(quotient) + " " + "{{ 'millions'|trans }}";
                if (quotient > 1 && reste != 0) numberToLetter = NumberToLetter(quotient) + " " + "{{ 'millions'|trans }}" + " " + NumberToLetter(reste);
                break;
            case 9:
                quotient = Math.floor(nb / 1000000);
                reste = nb % 1000000;
                if (quotient == 1 && reste == 0) numberToLetter = "{{ 'un million'|trans }}";
                if (quotient == 1 && reste != 0) numberToLetter = "{{ 'un million'|trans }}" + " " + NumberToLetter(reste);
                if (quotient > 1 && reste == 0) numberToLetter = NumberToLetter(quotient) + " " + "{{ 'millions'|trans }}";
                if (quotient > 1 && reste != 0) numberToLetter = NumberToLetter(quotient) + " " + "{{ 'millions'|trans }}" + " " + NumberToLetter(reste);
                break;
            case 10:
                quotient = Math.floor(nb / 1000000000);
                reste = nb - quotient * 1000000000;
                if (quotient == 1 && reste == 0) numberToLetter = "un milliard";
                if (quotient == 1 && reste != 0) numberToLetter = "un milliard" + " " + NumberToLetter(reste);
                if (quotient > 1 && reste == 0) numberToLetter = NumberToLetter(quotient) + " milliards";
                if (quotient > 1 && reste != 0) numberToLetter = NumberToLetter(quotient) + " milliards " + NumberToLetter(reste);
                break;
            case 11:
                quotient = Math.floor(nb / 1000000000);
                reste = nb - quotient * 1000000000;
                if (quotient == 1 && reste == 0) numberToLetter = "{{ 'un milliard'|trans }}";
                if (quotient == 1 && reste != 0) numberToLetter = "{{ 'un milliard'|trans }}" + " " + NumberToLetter(reste);
                if (quotient > 1 && reste == 0) numberToLetter = NumberToLetter(quotient) + " " + "{{ 'milliards'|trans }}";
                if (quotient > 1 && reste != 0) numberToLetter = NumberToLetter(quotient) + " " + "{{ 'milliards'|trans }}" + " " + NumberToLetter(reste);
                break;
            case 12:
                quotient = Math.floor(nb / 1000000000);
                reste = nb - quotient * 1000000000;
                if (quotient == 1 && reste == 0) numberToLetter = "{{ 'un milliard'|trans }}";
                if (quotient == 1 && reste != 0) numberToLetter = "{{ 'un milliard'|trans }}" + " " + NumberToLetter(reste);
                if (quotient > 1 && reste == 0) numberToLetter = NumberToLetter(quotient) + " " + "{{ 'milliards'|trans }}";
                if (quotient > 1 && reste != 0) numberToLetter = NumberToLetter(quotient) + " " + "{{ 'milliards'|trans }}" + " " + NumberToLetter(reste);
                break;
            case 13:
                quotient = Math.floor(nb / 1000000000000);
                reste = nb - quotient * 1000000000000;
                if (quotient == 1 && reste == 0) numberToLetter = "un billion";
                if (quotient == 1 && reste != 0) numberToLetter = "un billion" + " " + NumberToLetter(reste);
                if (quotient > 1 && reste == 0) numberToLetter = NumberToLetter(quotient) + " billions";
                if (quotient > 1 && reste != 0) numberToLetter = NumberToLetter(quotient) + " billions " + NumberToLetter(reste);
                break;
            case 14:
                quotient = Math.floor(nb / 1000000000000);
                reste = nb - quotient * 1000000000000;
                if (quotient == 1 && reste == 0) numberToLetter = "un billion";
                if (quotient == 1 && reste != 0) numberToLetter = "un billion" + " " + NumberToLetter(reste);
                if (quotient > 1 && reste == 0) numberToLetter = NumberToLetter(quotient) + " billions";
                if (quotient > 1 && reste != 0) numberToLetter = NumberToLetter(quotient) + " billions " + NumberToLetter(reste);
                break;
            case 15:
                quotient = Math.floor(nb / 1000000000000);
                reste = nb - quotient * 1000000000000;
                if (quotient == 1 && reste == 0) numberToLetter = "un billion";
                if (quotient == 1 && reste != 0) numberToLetter = "un billion" + " " + NumberToLetter(reste);
                if (quotient > 1 && reste == 0) numberToLetter = NumberToLetter(quotient) + " billions";
                if (quotient > 1 && reste != 0) numberToLetter = NumberToLetter(quotient) + " billions " + NumberToLetter(reste);
                break;
        }//fin switch
        /*respect de l'accord de quatre-vingt*/
        if (numberToLetter.substr(numberToLetter.length - "quatre-vingt".length, "quatre-vingt".length) == "quatre-vingt") numberToLetter = numberToLetter + "s";

        return numberToLetter;
    }//-----------------------------------------------------------------------

    /*****end Script de conversion le prix en lettre*****/
</script>
</body>
</html>