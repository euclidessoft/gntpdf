{% extends
    is_granted('ROLE_ADMIN') ? 'admin.html.twig' :
    is_granted('ROLE_SUPERVISEUR') ? 'admin.html.twig' :
    is_granted('ROLE_RH') ? 'rh.html.twig' :
    is_granted('ROLE_FINANCE') ? 'finance.html.twig' :
    is_granted('ROLE_CAISSIER') ? 'caisse.html.twig' :
    is_granted('ROLE_STOCK') ? 'stock.html.twig' :
    is_granted('ROLE_LIVREUR') ? 'livreur.html.twig' :
    is_granted('ROLE_EMPLOYER') ? 'employe.html.twig' :
    is_granted('ROLE_LABORATOIRE') ? 'laboratoire.html.twig' :
    'layout.html.twig'
%}
{% block title %} {{ "Liste des bulletins"|trans }} - GNT Pharma {% endblock %}
{% block salaire %}active{% endblock %}
{% block paie_salaire %}active{% endblock %}

{% block body %}
    <div class="col-lg-10 mt-4 fond-7">
        <h3 class="mt-4 mb-5 fw-bold">
            <i class="fa fa-money-bill me-2"></i>
            {{ "En attente de paiement"|trans }}
        </h3>
        {% for message in app.flashes('notice') %}
            <div role="alert" class="alert alert-success alert-dismissible fade show radius-10 shadow-black">
                <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                {{ message|trans }}
            </div>
        {% endfor %}
        {% for message in app.flashes('danger') %}
            <div role="alert" class="alert alert-danger alert-dismissible fade show radius-10 shadow-black">
                <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                {{ message|trans }}
            </div>
        {% endfor %}
        <div id="flash"></div>

        <table class="table table-success-light table-bordered table-hover table-striped my-3" id="liste">
            <thead>
            <tr>
                <th>{{ 'Matricule'|trans }}</th>
                <th>{{ 'Employé'|trans }}</th>
                <th>{{ 'Date de validation'|trans }}</th>
                <th>{{ 'Base Salariale'|trans }}</th>
                <th>{{ 'Net à Payer'|trans }}</th>
                <th>{{ 'Charge Salariale'|trans }}</th>
                <th>{{ 'Charge patronale'|trans }}</th>
                <th>{{ 'Action'|trans }}</th>
            </tr>
            </thead>
            <tbody>
            {% set montant = 0 %}
            {% set chargeSalarial = 0 %}
            {% set chargePatronal = 0 %}
            {% for paie in paies %}
                <tr id="tr{{ paie.id }}">
                    <td>{{ paie.employe.matricule }}</td>
                    <td>{{ paie.employe.nom }} {{ paie.employe.prenom }}</td>
                    <td>{{ paie.date|date('d/m/Y') }}</td>
                    <td>{{ paie.salaireBase|number_format(0, app.request.locale == 'fr' ? ',' : '.', app.request.locale == 'fr' ? ' ' : ',') }} FCFA</td>
                    <td>{{ paie.salaireNet|number_format(0, app.request.locale == 'fr' ? ',' : '.', app.request.locale == 'fr' ? ' ' : ',') }} FCFA</td>
                    <td>{{ paie.totalChargeEmploye|number_format(0, app.request.locale == 'fr' ? ',' : '.', app.request.locale == 'fr' ? ' ' : ',') }} FCFA</td>
                    <td>{{ paie.totalchargepatronal|number_format(0, app.request.locale == 'fr' ? ',' : '.', app.request.locale == 'fr' ? ' ' : ',') }} FCFA</td>
                    <td>
                        <button type="button" class="btn btn-green" data-bs-toggle="modal" data-bs-target="#modal{{ paie.id }}" id="btn{{ paie.id }}">
                            <i class="fa fa-plus"></i>
                            {{ 'Payer'|trans }}
                        </button>
                        <!-- Modal -->
                        <div class="modal fade" id="modal{{ paie.id }}" tabindex="-1" tabindex="-1" aria-labelledby="" aria-hidden="true"
                             aria-labelledby="exampleModalLabel" aria-hidden="true">
                            <div class="modal-dialog modal-dialog-centered text-center">
                                <div class="modal-content">
                                    <div class="modal-header">
                                        <h4 class="modal-title fs-5" id="exampleModalLabel">
                                            {{ 'Paiement salaire'|trans }}
                                        </h4>
                                        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                                    </div>
                                    <div class="modal-body px-5">
                                        <div class="mt-4">
                                            <label for="banque{{ paie.id }}"></label>
                                            <select class="form-select" id="banque{{ paie.id }}">
                                                <option value="0">Selectionnez une banque</option>
                                                {% for banque in banques  %}
                                                <option value="{{ banque.id }}">{{ banque.nom }}</option>
                                                {% else %}
                                                {% endfor %}
                                            </select>
                                        </div>

                                    </div>
                                    <div class="modal-footer px-5 py-4">
                                        <button type="button" class="btn btn-green me-3"  id="valider{{ paie.id }}"
                                                onclick="payerOne({{ paie.id }})">
                                            <i class="fa fa-save"></i>
                                            {{ 'Valider'|trans }}
                                        </button>
                                        <button type="button" class="btn btn-success-light" data-bs-dismiss="modal"
                                                id="annuler{{ paie.id }}">
                                            <i class="fa-solid fa-rotate-left"></i>
                                            {{ 'Annuler'|trans }}
                                        </button>
                                    </div>
                                </div>
                            </div>
                        </div>

                    </td>
                    {% set montant = montant + paie.salairenet %}
                    {% set chargeSalarial = chargeSalarial + paie.totalChargeEmploye %}
                    {% set chargePatronal = chargePatronal + paie.totalchargepatronal %}
                </tr>
            {% endfor %}
         
            </tbody>
        </table>
           {% if paies|length > 1 %}
           <table class="table table-success-light table-bordered table-hover table-striped my-3">
             <tr>
          <th class="text-center">
                 {{ "Salaires Nets"|trans }}: {{ montant|number_format(0, app.request.locale == 'fr' ? ',' : '.', app.request.locale == 'fr' ? ' ' : ',') }} FCFA
                   
                </th>
          <th class="text-center">
                   {{ "Charges Salariales"|trans }}: {{ chargeSalarial|number_format(0, app.request.locale == 'fr' ? ',' : '.', app.request.locale == 'fr' ? ' ' : ',') }} FCFA
                   
                </th>
                <th class="text-center">
                   {{ "Charges Patronales"|trans }}: {{ chargePatronal|number_format(0, app.request.locale == 'fr' ? ',' : '.', app.request.locale == 'fr' ? ' ' : ',') }} FCFA
                   
                </th>
                 <th class="text-center">
                   {{ "Total"|trans }}: {{ ( montant + chargePatronal + chargeSalarial)|number_format(0, app.request.locale == 'fr' ? ',' : '.', app.request.locale == 'fr' ? ' ' : ',') }} FCFA
                   
                </th>
                <th>
                    <button type="button" class="btn btn-green" data-bs-toggle="modal" data-bs-target="#modal_tous" id="tous">
                        <i class="fa fa-plus"></i>
                        {{ 'Payer tous'|trans }}
                    </button>
                    <!-- Modal -->


                </th>
            </tr>
            </table>
            {% endif %}
    </div>
    <div class="modal fade" id="modal_tous" tabindex="-1" tabindex="-1" aria-labelledby="" aria-hidden="true"
         aria-labelledby="exampleModalLabel" aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered text-center">
            <div class="modal-content">
                <div class="modal-header">
                    <h4 class="modal-title fs-5" id="exampleModalLabel">
                        {{ 'Paiement de tous les salaires'|trans }}
                    </h4>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body px-5">
                    <div class="mt-4">
                        <label for="{#mois{{ mois.id }}#}"></label>
                        <select class="form-select" id="banquetous">
                            <option value="0">Selectionnez une banque</option>
                            {% for banque in banques  %}
                                <option value="{{ banque.id }}">{{ banque.nom }}</option>
                            {% else %}
                            {% endfor %}

                        </select>
                    </div>

                </div>
                <div class="modal-footer px-5 py-4">
                    <button type="button" class="btn btn-green me-3"  id="validertous"
                            onclick="payerTous()">
                        <i class="fa fa-save"></i>
                        {{ 'Valider'|trans }}
                    </button>
                    <button type="button" class="btn btn-success-light" data-bs-dismiss="modal" id="annulertous">
                        <i class="fa-solid fa-rotate-left"></i>
                        {{ 'Annuler'|trans }}
                    </button>
                </div>
            </div>
        </div>
    </div>
    {% block scripts %}
        <script>
            function payerOne(id) {
                var banque = $("#banque"+id+" option:selected");
                if(banque.val() != "0") {
                    if (confirm("{{ 'Voulez-vous vraiement effetuer le paiement' }}")) {
                        $("#btn" + id).html("<i class='fa-solid fa-spinner fa-spin-pulse fa-lg'></i>");
                        $("#valider" + id).html("<i class='fa-solid fa-spinner fa-spin-pulse fa-lg'></i>");
                        $.ajax({
                            type: "POST",
                            url: "{{ path('finance_payer') }}",
                            data: { paie : id, banque : banque.val() },
                            success: function (data) {
                                if (data['id'] == 'ok') {
                                    $("#annuler" + id).trigger('click');
                                    document.location.href = "{{ path('finance_salaire') }}";

                                }
                            },
                            error: function () {
                                alert('{{ "La requête n'a pas abouti"|trans }}');
                            }
                        });
                    }
                }else alert("{{ 'Selectionnez une banque'|trans }}");

            }

            function payerTous() {
                var banque = $("#banquetous option:selected");
                var montant = $("#montant");
                if(banque.val() != "0") {
                    if (confirm("{{ 'Voulez-vous vraiement effetuer le paiement' }}")) {
                        $("button").html("<i class='fa-solid fa-spinner fa-spin-pulse fa-lg'></i>");
                        $.ajax({
                            type: "POST",
                            url: "{{ path('finance_payerTous') }}",
                            data: { banque : banque.val(), montant : montant.val() },
                            success: function (data) {
                                if (data['id'] == 'ok') {
                                    $("#annulertous").trigger('click');
                                    document.location.href = "{{ path('finance_salaire') }}";

                                }
                            },
                            error: function () {
                                alert('{{ "La requête n'a pas abouti"|trans }}');
                            }
                        });
                    }
                }else alert("{{ 'Selectionnez une banque'|trans }}");
            }
        </script>
    {% endblock %}
{% endblock %}
