{% extends
    is_granted('ROLE_ADMIN') ? 'admin.html.twig' :
    is_granted('ROLE_SUPERVISEUR') ? 'admin.html.twig' :
    is_granted('ROLE_RH') ? 'rh.html.twig' :
    is_granted('ROLE_FINANCE') ? 'finance.html.twig' :
    is_granted('ROLE_CAISSIER') ? 'caisse.html.twig' :
    is_granted('ROLE_STOCK') ? 'stock.html.twig' :
    is_granted('ROLE_LIVREUR') ? 'livreur.html.twig' :
    is_granted('ROLE_EMPLOYER') ? 'employe.html.twig' :
    is_granted('ROLE_LABORATOIRE') ? 'laboratoire.html.twig' :
    'layout.html.twig'
%}
{% block administration %}active{% endblock %}
{% block contrat %}active{% endblock %}
{% block body %}


    <div class="col-lg-5 offset-lg-2 mt-4 fond-7 pb-5 pt-3 px-5">
        <h4 class="fw-bold mb-5">
            <i class="fa fa-edit me-2"></i>
            {{ 'Modifier le contrat'|trans }}
        </h4>

        {{ include('contrat/admin/_form.html.twig') }}

        <div class="col-md-7 fond-5">
            <div class="centrer gap-3">
                {#                <div class="row">#}
                <button type="button" class="btn btn-shadow btn-green btn-normal" id="send" onclick="envoie()">
                    <i class="fa fa-save"></i>
                    {{ 'Enregistrer'|trans }}
                </button>

                <a id="{{ contrat.id }}" onclick="load({{ contrat.id }})" href="{{ path('contrat_index') }}"
                   class="btn btn-success-light btn-normal">
                    <i class="fa fa-undo"></i>
                    {{ 'Retour'|trans }}
                </a>
{#                {{ form_rest(form) }}#}
                {#                </div>#}
            </div>
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}
