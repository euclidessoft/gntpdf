<form method="post" action="{{ path('commande_panier_valider') }}" onsubmit="return confirm('Êtes-vous sûr de vouloir valider?');" class="d-inline-block">
    <input type="hidden" name="_token" value="{{ csrf_token('delete') }}">
    <button class="ms-3 btn btn-success float-end">
                    <i class="fa fa-money-bill"></i>{{ "Valider"|trans }}</button>
</form>
