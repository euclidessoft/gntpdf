<form method="post" action="{{ path('app_accompte_delete', {'id': accompte.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer cet element?');" style="display:inline;">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ accompte.id) }}">
    <button class="btn btn-danger"><i class="fa fa-trash"></i></button>
</form>
