<form method="post" action="{{ path('heure_suplementaire_delete', {'id': heure_suplementaire.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer?');" style="display:inline;">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ heure_suplementaire.id) }}">
    <button class="btn btn-danger"><i class="fa fa-trash"></i> </button>
</form>
