{% extends
    is_granted('ROLE_ADMIN') ? 'admin.html.twig' :
    is_granted('ROLE_SUPERVISEUR') ? 'admin.html.twig' :
    is_granted('ROLE_RH') ? 'rh.html.twig' :
    is_granted('ROLE_FINANCE') ? 'finance.html.twig' :
    is_granted('ROLE_CAISSIER') ? 'caisse.html.twig' :
    is_granted('ROLE_STOCK') ? 'stock.html.twig' :
    is_granted('ROLE_LIVREUR') ? 'livreur.html.twig' :
    is_granted('ROLE_EMPLOYER') ? 'employe.html.twig' :
    is_granted('ROLE_LABORATOIRE') ? 'laboratoire.html.twig' :
    'layout.html.twig'
%}
{% block title %} {{ "approvisionnement"|trans }} - GNT Pharma {% endblock %}
{% block fournisseur %}active{% endblock %}
{% block all_fournisseurs %}active{% endblock %}

{% block body %}
<div class="container mt-4 fond-7">
   <h3 class="mt-4 fw-bold">
        <i class="fas fa-chart-line me-2"></i>
        {{ "Analyse Fournisseur"|trans }} | {{ user.designation}} 
    </h3>
    <h5 class="mt-4 mb-5 fw-bold">
        <i class="fa fa-shopping-cart me-2"></i>
        {{ "Palmares des achats d'Articles"|trans }}
    </h5>
     <ul class="nav nav-tabs mb-4">
        {# {% if is_granted('ROLE_STOCK') %} #}
        <li class="nav-item">
            <a class="nav-link" href="{{ path('vente_chiffre_fourniseur', {'fournisseur':user.id}) }}">
                {{ "Chiffre d'Affaire"|trans }}
            </a>
        </li>   
        <li class="nav-item">
            <a class="nav-link active" href="#">
                {{'Articles'|trans}}
            </a>
        </li>
    </ul>
     <table class="table table-success-light table-bordered table-hover table-striped mb-3">
        <thead>
        
       <tr>
            <th>{{ produit.reference }}</th>
            <th>{{ produit.desigantion }}</th>
            <th>{{ produit.dci }}</th>
            <th>{{ produit.fabriquant }}</th>
        </tr
        <tr>
        <td colspan="4"></td>
        </tr>
         <tr>
            <th colspan="2"  class="text-center"><b>{{ "Quantité Totale achetée"|trans}}: {{ quantite|number_format(0, '', ' ') }}</b></th>
            <th colspan="2"  class="text-center"><b>{{ "Chiffre d'affaire"|trans}}: {{ montant|number_format(2, app.request.locale == 'fr' ? ',' : '.', app.request.locale == 'fr' ? ' ' : ',') }}</b></th>
        </tr>
        </thead>
        
    </table>
   

    <table class="table table-success-light table-bordered table-hover table-striped my-3">
        <thead>
        
        <tr>
            <th>{{ 'Date'|trans }}</th>
            <th>{{ 'Approvisionnement'|trans }}</th>
            <th>{{ 'Quantité achetée'|trans }}</th>
        </tr>
        </thead>
        <tbody>
        {% for produit in ventes %}
            <tr>
                <td>{{ produit.approvisionner.date|date("d/m/Y") }}</td>
                <td>
                 <a id="b{{ produit.approvisionner.id }}" onclick="load('b{{ produit.approvisionner.id }}')" href="{{ path('stock_show', {'id': produit.approvisionner.id}) }}" class="btn btn-success">
                 {{ produit.approvisionner.id }}
                </a>
                </td>
                <td>{{ produit.quantite|number_format(0,',' , ' ') }}</td>

            </tr>
        {% endfor %}
        </tbody>
        <tr>
            <th colspan="2"  class="text-center">{{ "Quantité Totale achetée"|trans}}: {{ quantite|number_format(0, '', ' ') }}</th>
            <th  class="text-center">{{ "Chiffre d'affaire"|trans}}: {{ montant|number_format(0, '', ' ') }}</th>
        </tr>
    </table>
    <div class="centrer gap-3  m-4 fond-7">
             <table class="table table-success-light table-bordered table-hover table-striped my-3" id="facture" style="display:none;">
        <thead>
        <tr>
            <th>{{ 'Date'|trans }}</th>
            <th>{{ 'approvisionnement'|trans }}</th>
            <th>{{ 'Quantité achetée'|trans }}</th>
        </tr>
        </thead>
        <tbody>
        {% for produit in ventes %}
            <tr>
                <td>{{ produit.approvisionner.date|date("d/m/Y") }}</td>
                <td>  <a id="b{{ produit.approvisionner.id }}" onclick="load('b{{ produit.approvisionner.id }}')" href="{{ path('stock_show', {'id': produit.approvisionner.id}) }}" class="btn btn-success">
                 {{ produit.approvisionner.id }}
                </td>
                <td>{{ produit.quantite }}</td>

            </tr>
        {% endfor %}
        </tbody>
    </table>
      <span id="exportexcel" class=""></span>
        </div>
    </div>
</div>
{% endblock %}
